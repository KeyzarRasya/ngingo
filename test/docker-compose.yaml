version: '1.0'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  web2:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001

  web3:
    build: .
    ports:
      - "3002:3002"
    environment:
      - PORT=3002